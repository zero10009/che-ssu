function creditScene() {
  push();
  rectMode(CENTER);
  fill(0);
  noStroke();
  square(width / 2, height / 2, 1440);
  imageMode(CENTER);
  image(creditPic, width / 2, height / 2);
  pop();
}
